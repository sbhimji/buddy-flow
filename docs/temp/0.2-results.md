# 0.2 verification results — 2026-08-11

Run late-window (~10:26–10:49 ET) rather than at the 09:31 slot in the runbook; the tape
was live throughout, which is what the checks require. Timings noted per step.

## verify-universe: PASS (190/190; BESIY flagged as expected)

Run ~10:26 ET. All 190 equity symbols resolved; BESIY flagged `market=otc` (expected —
that flag alone is fine per runbook); conditions dump rewritten; historical probe OK.

```
Checking 190 symbols against https://api.massive.com/v3/reference/tickers ...

Dumping stocks condition codes (with update_rules) ...
  94 conditions; by type: map[financial_status_indicator:10 market_condition:2 quote_condition:33 sale_condition:40 settlement_condition:2 short_sale_restriction_indicator:4 sip_generated_flag:2 trade_thru_exempt:1]
  wrote ../../docs/foundations/massive-conditions.json (seed for the 0.3 print-inclusion table)

Historical probe: 1-min bars for NVDA on 2026-08-10 ...
  938 bars returned (regular session alone = 390)

===== SUMMARY =====
Symbols resolved: 190/190
FLAGGED (non-stocks market or inactive — verify these stream on the SIP websocket):
  BESIY (market=otc)
RESULT: OK
```

## verify-feed NVDA: PASS (trades=42292, quotes=33238, latency p50=60 / p95=104 / p99=148 ms)

Run ~10:28–10:33 ET, 5-minute window. Connected, entitled, streaming on both channels.
Latency p95 = 104ms against the 5000ms print→pixel budget — no concern.

Note on the empty "Gateway/status messages" section: the massivews client performs the
auth + subscription handshake internally — `Connect()` fails outright on a bad key, and
data flowing on both subscribed channels proves the subscriptions took. Entitlement is
verified by behavior; the acks are just not surfaced as raw messages to the script.

```
Connected. Watching NVDA (trades+quotes) for 5m0s ...

===== RESULTS: NVDA =====

Gateway/status messages (auth + subscription acks = entitlement):

Messages: trades=42292 quotes=33238
SIP->receipt latency ms: p50=60 p95=104 p99=148 max=513  (budget: 5000 print->pixel)

RESULT: OK — connected, entitled, streaming
```

## BESIY probe: does not stream (trades=0, quotes=0 across 7 minutes of subscription)

Two probes (2m at ~10:34, then a 5m confirmation at ~10:37 since the run was off the
open window): **zero trades and zero quotes on both**. REST cross-checks make the
verdict firm rather than "thinly traded, maybe unlucky":

- **BESIY did print during the observation period.** `v3/trades` shows prints at
  10:32:51, 10:36:39, 10:37:18, 10:44:57, 10:45:05 ET (sizes 1–60, tape 3 = OTC,
  exchange 62 = OTC Markets), i.e. prints occurred in/around the probe windows and
  never appeared on the websocket trade channel.
- **The vendor has no quote data for BESIY at all.** `v3/quotes/BESIY` returns zero
  results even unfiltered — OTC symbols are off the SIP NBBO, so the quote channel can
  never deliver, which also means Lee-Ready classification is impossible for BESIY
  regardless of streaming.

Observation recorded; keep/drop/replace is the trader's decision (D1). Relevant facts
for that decision: OTC prints are REST-only at this vendor, no NBBO exists, and today's
prints were 1–60 shares — negligible dollar volume for flow metrics.

```
Connected. Watching BESIY (trades+quotes) for 5m0s ...
  ... trades=0 quotes=0   (×9)

===== RESULTS: BESIY =====
Messages: trades=0 quotes=0
RESULT: INCOMPLETE — no trades or no quotes (market closed? entitlement? see status messages above)
```

## Verdict: 0.2 closed

Universe resolves 190/190, feed is entitled and streaming with latency two orders of
magnitude inside budget, historical access works, condition codes dumped for 0.3.
Deferred items (failover feed, ZN/ZB bond tape, BESIY disposition) carried on the
DEV-PLAN 0.2 entry.
